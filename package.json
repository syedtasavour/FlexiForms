{
  "name": "flexiforms",
  "version": "2.0.0",
  "description": "Dynamic Form Builder Application",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install",
    "install-all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/flexiforms.git"
  }
}
